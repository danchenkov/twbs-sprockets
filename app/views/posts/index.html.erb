<div class="container">
  <p style="color: green"><%= notice %></p>

  <h1>Posts</h1>
  <p>This is a regular paragraph.</p>
  <p class="special">This is a special note to test styles.</p>
  <p>This is a regular paragraph.</p>

  <div id="posts" class="listing">
    <% @posts.each do |post| %>
      <%= render post %>
    <% end %>
  </div>

  <%= link_to "New post", new_post_path, class: "btn btn-primary"  %>
</div>
